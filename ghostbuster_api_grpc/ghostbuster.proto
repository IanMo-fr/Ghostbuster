syntax = "proto3";

service Ghostbuster {
  // Méthode RPC pour tester la connectivité
  rpc SayHello (HelloRequest) returns (HelloResponse);
}

message HelloRequest {
  string name = 1;
}

message HelloResponse {
  string message = 1;
}
